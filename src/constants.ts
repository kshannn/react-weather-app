export const SEARCH_HISTORY_TIME_FORMAT = `DD MMM YYYY, hh:mm:ss A`;

export const CURRENT_RESULT_TIME_FORMAT = `YYYY-MM-DD hh:mm A`;

export enum HTTP_STATUS {
  "OK" = 200,
}

export const NUM_OF_ITEMS_PER_PAGE = 3;
